#pragma once
#ifndef __RANDOM_H_INCLUDED__   // if x.h hasn't been included yet...
#define __RANDOM_H_INCLUDED__   //   #define this so the compiler knows it has been included
int randnum(int);
#endif